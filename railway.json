{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd Project && pip install -r ../requirements.txt && python manage.py collectstatic --no-input"
  },
  "deploy": {
    "startCommand": "cd Project && python manage.py migrate --no-input && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
